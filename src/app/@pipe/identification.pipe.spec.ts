import { IdentificationPipe } from './identification.pipe';

describe('Pipe: Identification', () => {
  it('create an instance', () => {
    let pipe = new IdentificationPipe();
    expect(pipe).toBeTruthy();
  });
});
